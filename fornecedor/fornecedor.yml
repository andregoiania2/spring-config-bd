spring:
  datasource:
    password: root
    url: jdbc:postgresql://172.29.0.2:5432/fornecedor
    username: root
  jpa:
    hibernate:
      ddl-auto: update
    jdbc:
     lob:
       non_contextual_creation:
         true
    properties:
      hibernate:
        format_sql: true
        show_sql: true 
        dialect:
          org.hibernate.dialect.PostgreSQLDialect
  sql:
    init:
      mode=always   

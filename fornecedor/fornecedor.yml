spring:
  datasource:
    password: root
    url: jdbc:postgresql://172.29.0.2:5432/fornecedor
    username: root
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
        show_sql: true 
  sql:
    init:
      mode=always   
